// @ts-ignore
import { config } from 'config';

export class EmailService {

    /**
     * @description Sign given payload
     * @author DerZade
     * @param {string} email Email to send verification mail
     * @param {string} code Verification code
     */
    public static sendVerificationMail(email: string, code: string) {
        // TODO
    }
}
